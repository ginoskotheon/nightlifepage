<!--<div id="loggingin">
  <a href='/login' id="log"><i class="fa fa-sign-in fa-2x" aria-hidden="true"> LOGIN</i></a>
</div>-->
<h1 id='whats'>What's on in {{data.businesses.0.location.city}}! </h1>

  {{#each data.businesses}}
  <div class='yelp-output'>
    <div class='yelp-item'>
      <div class='yelp-box'>
        <img class="yelp-thumbnail" src="{{this.image_url}}">
        <h2 class="business-name">{{ this.name }}</h2>

        <form class="pregoing" id="pregoing">
        <a href="/auth/twitter"><input class="predecision" id="predecision" type="button" name='preuserAttendingVenue' value="Not Going" /></a>
        </form>
        <div class="preinfo">
          <p class="business-description">{{this.snippet_text }}</p>
          <p class="business-address">{{this.location.display_address}}</p>
        </div>



      </div>
    </div>
  </div>
  <hr />
{{/each}}
